services:
  - type: web
    name: smart-energy-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: "8000"
    healthCheckPath: /health
    plan: free
