services:
  - type: web
    name: smart-energy-backend
    env: docker
    region: oregon
    plan: free # Note: The free tier might crash with PyTorch/RL due to memory limits.
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
